# grille-engine

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build grille-engine` to build the library.

## Running unit tests

Run `nx test grille-engine` to execute the unit tests via [Vitest](https://vitest.dev/).
